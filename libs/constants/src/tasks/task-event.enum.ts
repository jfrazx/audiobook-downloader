export enum TaskEvent {
  CreateMany = 'task.create.many',
  RemoveMany = 'task.remove.many',
  Create = 'task.create',
  Update = 'task.update',
  Remove = 'task.remove',
  FindAll = 'task.find.all',
  FindOne = 'task.find.one',
}
