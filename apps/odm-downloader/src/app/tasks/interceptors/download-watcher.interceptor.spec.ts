import { DownloadWatcherInterceptor } from './download-watcher.interceptor';

describe('DownloadWatcherInterceptor', () => {
  it('should be defined', () => {
    expect(new DownloadWatcherInterceptor()).toBeDefined();
  });
});
