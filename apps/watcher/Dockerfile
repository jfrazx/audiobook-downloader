# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build watcher`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t watcher`.
FROM docker.io/node:lts-alpine

WORKDIR /app

RUN addgroup --system watcher && \
          adduser --system -G watcher watcher

COPY dist/apps/watcher watcher/
RUN chown -R watcher:watcher .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix watcher --omit=dev -f install

CMD [ "node", "watcher" ]
