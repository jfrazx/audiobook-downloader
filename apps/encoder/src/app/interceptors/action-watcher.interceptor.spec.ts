import { ActionWatcherInterceptor } from './action-watcher.interceptor';

describe('ActionWatcherInterceptor', () => {
  it('should be defined', () => {
    expect(new ActionWatcherInterceptor()).toBeDefined();
  });
});
